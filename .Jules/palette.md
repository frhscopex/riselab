## 2024-05-20 - Missing ARIA Labels in JS Templates
**Learning:** Accessibility attributes like ARIA labels are often missed when UI components are dynamically generated via JavaScript template strings.
**Action:** When adding ARIA labels to HTML, also grep for the same classes/icons in JS files to ensure dynamically created elements are also accessible.
